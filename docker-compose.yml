services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: supportvision
      POSTGRES_PASSWORD: supportvision
      POSTGRES_DB: supportvision
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
